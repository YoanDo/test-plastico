import styled from 'styled-components';

export const MapWrapper = styled.div`
  margin: 0 auto;
  position: relative;
  width: fit-content;
  .map-container {
    height: 60vh;
    width: 100vw;
  }
  .sideBar {
    position: absolute;
    width: fit-content;
  }
`;
